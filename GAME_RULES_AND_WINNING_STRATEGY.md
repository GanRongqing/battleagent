# 海战 Simulator — GAME RULES & WINNING STRATEGY RECONNAISSANCE

> 目标：不研究 Agent 架构，只回答"这个游戏怎样才能稳定打赢"。全部重要机制均给出**源码依据**和**实测结果**（探针脚本在 `/root/autodl-tmp/hsystem/probe_rules/`，引擎只读未改动，未跑长时间批量评估，未实现最终 Agent，未用黑方真值设计策略）。
>
> 一句话答案（后文逐节证明）：**白方把 15 艘 USV 编成 2v1（或 3v1）火力组，全速东进，对黑船集中锁定，每约 300–600 秒歼灭一艘，在首个黑船突防（t≈21000s）之前（约 t≈13000s）把 15 艘黑船全部打死，即必然胜利。** 不需要扇区、不需要阵型、不需要规避，UAV 是可选辅助。

---

## 0. 决胜公式（先看这个）

```
胜负 = 击杀竞速（看谁先攒满2发命中）
     → 2v1 白方每300s窗口64%直接击杀 / 96%至少冻结；
       1v1 需要约600–975s才能击杀。
     → 白方集中火力 → 每次互锁竞速必胜（实测3/3），每艘白USV至多吃1发冻结（不致死）。

时间预算 = 首个黑船突防线 x=50000 的期限 t=21000s（不被打断时）
         白方全歼15艘只需 t≈12500–14000s（提前8000s+）

结论 = "全速东进 + 2v1集中锁定 + 重复直到黑船全灭" 即可稳定取胜。
```

---

## 1. 完整还原胜负条件

### 源码依据
- 裁判系统 `judge_system.py` L87–94：黑船 `coords[0] <= 50000` → 记入 `black_success` 并 `unit.kill()`；`period=1s`。
- 引擎结束判断 `simulation/core/tzb_engine.py` L120–135（顺序就是优先级）：

```python
if total_white_alive == 0:          # 白方USV+UAV全灭
    ended = "我方所有单位被击毁"
elif black_ship_alive == 0:         # 黑方USV全灭
    ended = "敌方所有单位被击毁"
elif black_breakthrough > 0:        # 有黑船突防
    ended = "敌方N艘艇突破防线"
elif not isactive:                  # 到 end_time=600000s
    ended = "仿真引擎已停止"
```

- API 胜负映射 `pomdp_api/main.py` L1211–1243：
  - `"敌方所有…击毁"` → **Victory**
  - `"我方所有…击毁"` 或 `"…突破…"` → **Defeat**
  - 其余（引擎停止/超时）→ 按 `env_score_flat` 求和：`>0 → Victory`，`<0 → Defeat`，`=0 → Tie`。

### 关键结论
1. **正常胜利途径只有一条：在首个黑船突防前，把 15 艘黑船全部击毁**（`black_ship_alive==0` → Victory）。
2. **任何黑船在"其余黑船仍存活"时突防 → 立即 Defeat**。突防判定每 1s 一次，x≤50000 即判。
3. 白方全灭（15 USV + 15 UAV 全死）→ Defeat。
4. `env_score_flat` 全部字段（`black_killed/black_hit/white_ship_killed/white_uav_killed/black_breakthrough/white_hit/collision`）都是**只增不减的正计数**，求和恒 ≥0 → 若真打到超时，白方只要拿过 1 分就是 Victory（理论漏洞）。但实战到不了超时（黑船 21000s 先突防，白方若不歼灭必败；若歼灭则更早以"全歼"结束），所以该漏洞基本不可达。
5. **隐藏保底漏洞**：若**最后一艘**黑船突防并被裁判 `kill()`，此时 `black_ship_alive==0` 在 `black_breakthrough>0` **之前**被检查 → 引擎报"敌方所有单位被击毁" → **Victory**。即"最后一艘突防=白方仍赢"。这只是安全垫，不构成策略。

### 实测
- `P7a`：单黑船放在 x=49000，`update(3s)` → `black_success=1`、黑船被击杀、`ended_reason=敌方所有单位被击毁`（印证顺序漏洞）。
- `P7b`：frozen 黑船静止在 x=51500，200s 内不误判突防。

---

## 2. 完整还原地图

### 源码依据
- 任务区域 `sim_script/20250819TZB/Demo/任务区域.json`（8 顶点，笛卡尔坐标）：**x∈[0, 300000]，y∈[0, 673205]**。裁判 `judge_system.py` L105–112：单位离开区域多边形 → `unit.kill()`。
- 突防线 `destination = 50_000`（judge_system.py L27）。
- 兵力初始坐标 `sim_20250819测试用例1.py` L29–33、L67、L122–125：
  - 白方 15 USV：`x=0，y=300000+(i-1)*15000`（y 从 300000 到 510000），i=1..15。
  - 黑方 15 USV：`x=260000，同 y`。
  - 黑方 15 UAV：`x=260000，同 y，高100m`。
  - 白方 15 UAV：`x=0，同 y`，初始在船上（`put_uav_in_ship`）。

### 几何要点
- 白方贴西边界 x=0 出发（向东为正，不出区域）；黑方在东部 x=260000 向西压来。
- 双方 y 轴完全对齐（i 号对 i 号同 y 线），横向间隔 15km。
- 交战发生在 x≈130000–175000（远离 50000 突防线，天然留出巨大纵深）。

---

## 3. Ship 运动模型

### 源码依据
- `simulation/arsenal/tzb_motor.py`（`MotorTZB`）：`_move` 每 `period` 转一圈；转向按最短路弧、`real_angle ±= max_angle_velocity*period`；加速 `±max_acce*period`；`_ref_unit` 存在时（在母船）`_move` 直接 return（无人机在船不动）。
- DB（实测 dump）：
  - `ShipMotorTZB`: `max_speed=20, max_acce=2, max_angle_velocity=5, period=0.1`
  - `PlaneMotorTZB`: `max_speed=100`（官方脚本 L90 覆盖为 **150**）, `max_acce=10, max_angle_velocity=10, period=0.1`
- 初始航向：`gen_platform(..., speed, course, height)`，速度向量 `[speed*sin(course), speed*cos(course)]`，**方位 0=北、90=东**（`base_engine.pyx` speed2vel；`geometry.azimuth = atan2(dx,dy)`）。`heading = course = 速度方位角`（arch.pyx L420–424 / L1041–1055；platform.py L82）。
- `cmd_sail_area` 插入航路点，最后速度降到 0.0001（黑船到 x=0 后停下）。

### 实测（`P8`/`P11` 与 kinematics 探针）
- USV：指令 `speed=20, course=90` 后 ~10s 达 20m/s；转向 **恰好 5°/s**（90→180 用 36s）。
- 黑船：`cmd_sail_area(speed=10)` 严格 **10 m/s 正西**（1600s 位移 16000m，实测吻合）。
- 黑UAV：`cmd_sail_area(speed=30)` 正西 30 m/s；白UAV（起飞后）指令 `150/90` → 20s 内达 150 m/s（P5）。

### 关键：无人机 heading 跟随"速度方向"，不是指令角
- `motor.course`：`speed==0` 时返回 `_prev_course`（初值 0=北）；只有**真正移动**时 `course` 才等于速度方位（arch.pyx L1041–1055）。
- 实测：悬停无人机（`target_speed=0`）即使 `real_angle=90`，`heading` 仍 =0；一旦 `target_speed=150` 飞起来，heading 才 =90。
- **推论（对扇区型雷达极重要）：要让 UAV 的 ±30° 雷达对准某方向，必须让它朝那个方向飞，而不是"悬停+转向"。**

---

## 4. 敌方行为（完全确定性）

### 源码依据（`sim_20250819测试用例1.py` L119–176）
- 15 黑 USV：`[260000, y_i]`，`cmd_sail_area(speed=10, 西向航路点)` → **直线正西 10 m/s，永不转向、不规避**。
- 15 黑 UAV：`[260000, y_i]`，`cmd_sail_area(speed=30, 西向)`，t=0 起**已在空中**；电池从 t=0 起放电（`take_off_time=-1`），`total_fly_time=25000s` → t≈25000s 自毁。途中只朝西飞，不改航向。
- `black_strategy`（每 1s）：
  ```python
  detected = engine.get_black_targets()
  for _b in black_usvs:
      for _w in detected:
          if "usv" in _w: engine.black_cmd_lock(_b.name, _w)
  ```
  → 每个黑 USV 对**检测到的白 USV**逐个尝试锁定；受 `is_locking`（一艘只锁一个）约束，实际**每艘黑船 1v1 分散火力**，无集中、无优先级、无规避、无 UAV 指挥。

### 关键
- 黑方是**完全可预测的**：每艘黑船位置 = `260000 - 10·t`（同 y 线）。唯一随机性 = 锁定命中 80% 骰子。
- 黑 USV 锁定需目标在 `get_black_targets()`（=所有 BLUE 单位 intel 的并集，含黑 UAV 的 60km 探测）**且**距离 <40km。**黑 UAV 把黑船锁距从"黑船自身 30km 雷达"延长到 40km**（与白方对称，P10 实测 2/2）。

### 实测
- `P10`：黑 UAV 朝西飞（heading=270），白船在其 ±30° 扇区（方位≈243°）内被探测 → 黑 USV 在 35.2km（>黑船 30km 雷达）锁定白船成功。**黑方也有"UAV 延长锁距"。**

---

## 5. Detection / Radar 机制

### 源码依据（`arsenal/radar.pyx`）
- 探测方法 `fixed`：**无噪声、完全确定**（`dis < distance` 即探测到）。`acquire` L83–130 依次检查：isactive / is_on / 高度 z∈[0,10000] / 扇区（`sector + heading` 内）/ 距离（`dis<=max_distance`=300000，再 `dis<distance`）。
- 参数（DB dump）：
  - 白 USV 雷达：`distance=35000, sector=[0,360]`（`set_sector` 对 [0,360] 置 None = 全向）。
  - 黑 USV 雷达：`distance=30000, sector=[0,360]`（全向）。
  - UAV 雷达（双方）：`distance=60000, sector=[-30,30]`（**航向锚定** ±30°）。
- `_search` L267–278：候选 = 存活、`isdetectable`、异阵营、`Ship/Plane/Base/Submarine`。`_detect` L282–318：period=2s；首见为 point、`yield_time=2s` 后转 track；目标离开扇区后 `update_time=2s` 从雷达轨道移除；**intel 轨道在 `radar_update_time=30s` 后才删除**（`arsenal/intelligence.py`）。
- `get_white_targets()`（engine.py L246–272）= 所有 RED 单位 intel 目标并集 + 被黑锁/冻结的黑船。**任何一架 UAV 探测到黑船，就进入白方全体目标池 → 任意白 USV 可对其锁定（距离 ≤40km）**。

### 实测
- `P9`（UAV ±30° 扇区，航向锚定）：
  - 朝东 90°，扇区[60,120]：b_in 方位78° `acquire=True`；b_out 方位51° `acquire=False`。
  - 转向朝东北 45°，扇区[15,75]：b_out 方位51° `acquire=True`；b_in 方位80° `acquire=False`。
  - **30s 黏性**：转向后第 15s，已在扇区外的 b_in 仍出现在 `white_targets`（intel 未过期）；+31s 后过期消失。
- `P5`：UAV 朝东飞行 → 白 USV 在 **38.6km（>自身 35km 雷达）**锁定黑船成功（3/3）。
- `P10`：黑 UAV 同理延长黑方锁距（2/2）。

### 结论
- 探测周期 2s、轨道延迟 ≤4s、扇区外目标**最多黏性 30s**（小优势：短暂离开仍可见）。
- **UAV 探测是"全体共享"的**：白方雷达最强的白 USV 只有 35km，但 UAV 60km 探测让任意白 USV 能在 40km 锁距内锁任何被看见的黑船。

---

## 6. Lock / Combat 机制（本游戏最核心）

### 源码依据（`arsenal/locker.py`，逐行读）
- `is_locked` L66：攻击者对目标建链 `{locked_begin_time, locked_times:0, frozen:False, locked:True}`；**多个攻击者各自独立建链**（2v1 是两条独立 300s 窗口）。
- `_locked_work` L92（目标 locker 上每 1s 跑）：当 `time - locked_begin_time > 300`：
  - **无条件** 断链 `locked=False`，且 `emy_unit.locker.locking=False` → **攻击者被释放**（无论命中与否）。
  - 然后 `random.random() > 0.2`（**80% 命中**）→ `locked_times+=1; frozen=True; frozen_time=time`。
  - **`sum(locked_times) >= 2` → 击杀**（杀本船 + 机载无人机）。
- 锁距：`dist >= 40000` → 清 `locking`（脱锁）。
- **冻结**：motor 冻结（`_move` 直接 return）、雷达关、locker 关（`_turn_off`）；**300s 后自动解除**；**冻结中仍可被再次锁定**（`get_white_targets` 含 frozen）。

### 关键机制提炼
1. **击杀需要 2 发命中**（不是 3 发、不是血量）。
2. **每 300s 一个命中窗口**（严格 `>300`），窗口结束无论命中与否攻击者都被释放。
3. **第一发命中即冻结**：目标 300s 内不能动、不能开火、不能反击（雷达/锁定全关）。这是"先手冻结=彻底压制"的根源。
4. **2v1 = 两个独立 80% 窗口同时到点**：P(两发都中)=0.64 → 300s 直接击杀；P(一发)=0.32 → 冻结；P(零发)=0.04。→ **2v1 在 ~300s 击杀概率 64%，~600s 累计 ≈93%**。
5. **1v1 = 每窗口 80% 一发**：杀需 2 窗口 → 期望 ~750s（命中分布 600–975s）。
6. **3v1**：P(≥2 发)=1−(0.2³+3·0.8·0.2²)=**0.896** → ~90% 在 300s 击杀，几乎必然。
7. 冻结目标可被追刀（第二攻击者在冻结期锁定，冻结结束瞬间 300s 窗口叠加命中 → 击杀）。

### 实测汇总
- `P1`（2v1，12 次）：+310s 击杀 8/12（hits=2），冻结 4/12（hits=1）→ 与理论 0.64/0.32 吻合。
- `P2`（1v1，5 次）：首窗口命中+冻结 2/5 并在 +620s 击杀；miss→重锁→再 305s 命中 3/5（总 ~900s）。
- `P3`（5/5）：命中后攻击者释放，可立刻换锁新目标。
- `P4`（5/5）：冻结中追刀，+305s 击杀。
- `P6b`（2/2）：锁后瞬移 >40km → 锁立即断。
- `P11`（3/3，互锁竞速）：白 2v1 vs 黑 1v1，黑船 +350s 死亡，黑方只对白方 1 艘造成 1 发冻结，白船存活。

---

## 7. 时间线分析（默认 10 m/s 双方对进）

| t(sim) | 事件 | 依据 |
|---|---|---|
| 0 | 白 USV x=0（10 东）；黑 USV x=260000（10 西）；黑 UAV 空中（30 西） | 脚本 L83/125/146 |
| ≈5000 | 黑 UAV 60km 探测到白 USV（40 m/s 相向） | 260000−40t=60000 |
| ≈11000 | 双方互相进入 40km 锁距（靠 UAV 探测双方都能锁） | 260000−20t=40000 |
| ≈11250 | 白 USV 自身 35km 雷达首见黑船 | 260000−20t=35000 |
| ≈11500 | 黑 USV 自身 30km 雷达首见白船（**白方早 250s**） | 260000−20t=30000 |
| ≈11850 | 白 2v1 首杀（+350s 后） | P11 |
| ≈12500–14000 | 15 艘黑船全部歼灭（分波 2v1/3v1） | 估算 |
| ≈13000 | 相遇点 x=130000（默认 10 m/s） | 260000−20t=0 |
| **21000** | **首个黑船突防 x=50000 的期限（不被打断时）** | 260000−10t=50000 |
| 25000 | 黑 UAV 电池耗尽自毁 | UAV_Battery |

- **白方若全速 20 m/s 东进**：相遇点提前到 t≈8667s、x≈173000，歼灭更早，安全裕度更大（可选）。
- 白方歼灭完成 t≈13000s vs 突防期限 21000s → **~8000s 裕度**。时间充裕到"即使 1v1 分散也基本来得及"，因此**时间不是瓶颈，白方损失才是**（→ 见 §9）。

---

## 8. 拦截线

- **不需要在 x=50000 死守**——突防线根本不是"线防御"问题，而是"歼敌时限"问题：只要在 21000s 前杀光黑船，就没有黑船能到 50000。
- 最佳拦截地带：**x≈130000–175000**（双方中线以东），离突防线有 8–12 万米纵深。白方 USV 全速东进（20 m/s）即可把歼灭地带推到更东、更安全。
- 因为黑船被冻结就停止前进（300s/次），每发冻结都自动推迟突防；而 2v1 击杀又比冻结更快完成。**白方的作战区天然是"黑船全灭前，东半场"。**

---

## 9. 集中火力 vs 1v1（策略分水岭）

### 为什么集中必胜
- 击杀要 2 发。2v1 在 ~300s（64%）/~600s（93%）击杀；1v1 需 ~600–975s。
- **第一发命中即冻结**：白 2v1 的 +300s 第一发冻结黑船 → 黑船"反杀"白船的**第 2 发永远等不到**（黑船被冻 300s 无法开火）→ 白方 2 发先到，必胜。
- 实测 `P11`（3/3）：白 2v1 vs 黑 1v1 互锁，黑船 +350s 死，白船最多挨 1 发冻结（存活）。这就是"集中火力 vs 分散"的定量证据。
- 而黑方 `black_strategy` 就是**每艘 1v1 分散**——这正是它输的根源。

### 为什么 1v1 有风险
- 白方若 1v1 分散，与黑船对赌：双方同速攒 2 发，近似 50/50 竞速。白船被打死时，它锁的黑船若只吃了 1 发则被释放、继续西进 → 可能漏网突防。
- 15v15 的 1v1 并行虽也能在 ~1500s 杀光（时间够），但**白船损失大、有漏船风险**。集中火力把风险降到接近零。

### 建议兵力分配
- **2v1**：15 艘 → 7 组(2艘) + 1 备用，每组对一艘黑船；击杀后释放的空船转组/换目标。两到三波 ≈1200–1800s 全灭。
- **3v1（最稳）**：15 艘 → 5 组(3艘)，每组 90% 在 300s 击杀一艘，三波 ≈1000s 全灭；即使一艘被冻，同组还剩 2 艘继续。**首波用 3v1 清掉 5 艘最推荐**（瞬间减少黑方火力源）。

---

## 10. 敌我雷达差利用

| 项目 | 白 | 黑 | 差 |
|---|---|---|---|
| USV 雷达 | 35km 全向 | 30km 全向 | 白 +5km |
| UAV 雷达 | 60km ±30° | 60km ±30° | 平 |
| 锁距 | 40km | 40km | 平 |
| 击杀速率 | 2v1→300–600s | 1v1→600–975s | 白碾压 |
| 首见白船（自雷达） | t≈11250 | t≈11500 | 白早 250s |

- **真正可用的优势不是 5km 雷达，而是"UAV 探测 + 40km 锁距"让白方在最远距离上抢先锁定，加上集中火力在竞速中必胜。**
- 白 USV 自身 35km 雷达 + 40km 锁距 = **"看得见就锁得上"**（35<40，无锁隙）。黑 USV 30km 雷达 + 40km 锁距同理，但黑方只有 1v1。
- UAV 探测让白方锁 40km 外目标（P5 实测 38.6km 成功）→ 白方比"只用自身雷达"早 ~250s 开火；这 250s 把白方击杀再提前，进一步拉开与黑方反杀的差距。
- 30s 探测黏性：目标短暂离开扇区/范围仍可见（P9）→ 边际利于先手方。

---

## 11. UAV 的真正战略价值

**先给结论：UAV 对"打赢"不是必需项。** 白 USV 35km 雷达 + 40km 锁距已足够完成"探测→锁定→击杀"闭环（P5 证明了即使没有 UAV，白船在 35km 内也能锁）。去掉全部 UAV，USV-only 策略照样能赢（时间裕度充裕）。

UAV 的价值（可选增强）：
1. **提前 60km 探测 + 提前锁定（40km）**：比白 USV 自身雷达早 ~250s 开火，击杀更早、裕度更大（P5）。
2. **抵消黑 UAV 对黑方的同样增强**（P10 证明黑方也有此能力）：白 UAV 让白方不至于在"首锁"上落后。
3. 30s intel 黏性、无成本侦察（电池 25000s 够用整局）。
4. **局限**：±30° 扇区锚定速度方向（必须飞行转向，P9）；不能锁定/不能击杀；母船沉没则机毁；在船上雷达关闭（起飞后才有雷达）。

**最简单用法**：t=0 全部 15 架起飞，`fly(150, 90)` 朝东直飞，给舰队罩一个 60km 前向伞。不需要任何复杂航线。

---

## 12. 被锁定后的最佳反应

**结论：靠"拉开距离"逃跑不成立，正确反应是"接受冻结、让伙伴把攻击者打死"。**

### 为什么跑不掉（源码+实测）
- 锁只在 `dist>=40000` 时断（locker.py；P6b 实测 2/2 瞬移断锁）。
- 但 300s 命中窗口**先到**：白船以 20 m/s 全速逃，300s 只拉开 ~6km（P6 实测 +310s 仅到 20.7km，距 40km 还差一倍）；真实几何中黑船同向 10 m/s 西进，相对逃跑速度只有 10 m/s → 需要 500s+，远超 300s 窗口。**任何几何下 300s 骰子都先掷**。
- 结论：**逃跑保不住命**（P6：t0/t2 在 +310s 被命中冻结在 20.7km）。

### 正确反应
1. **根本别让自己处于 1v1**：确保锁定自己的黑船同时被 2 艘以上白船锁定（集中火力是防御，P11）。黑船被白 2v1 第一发冻结后，它的第 2 发反杀就永远不来 → 你最多挨 1 发冻结。
2. **挨冻结 = 活**：1 发不致死（要 2 发）；冻结 300s 后自动解除。期间不能动、不能开火，靠同组伙伴掩护。
3. 冻结解除后立即重新锁定（攻击者已释放，可换目标 P3）。
4. **注意间距**：冻结的白船是"再锁定靶"且不动——别让第二艘黑船进入它 40km 内补刀（把火力组纵向拉开即可）。

---

## 13. Sector（区域划分）是否必要

**不必要。** 获胜策略不需要任何扇区/区域划分/防御阵线：
- 白 USV 雷达全向 35km，唯一带扇区的是 UAV（±30°）。
- 即便用 UAV，也只须"朝东飞"——±30° 前向扇区天然覆盖从正东压来的黑舰队（P9 验证扇区确实锚定航向）。
- 不存在需要"守"的区域：突防线是时间期限（21000s 前杀光），不是需要占位的地方（§8）。
- 所以：**0 个扇区、0 个驻点、0 条防线，全部单位单向突进即可。**

---

## 14. 非作弊规则优势（白方合法白捡）

1. **击杀竞速碾压**：白 2v1 vs 黑 1v1，白必赢（P11）。黑 AI 不会集中，这是送分项。
2. **黑方全确定性**：每艘黑船位置 = `260000−10t`，可精确预测，无需侦察决策。
3. **+5km 雷达 / 早 250s 开火**（§10）。
4. **先手冻结=永久停船**：黑船第一发被冻结就停止西进 300s；每冻结一次突防延迟 300s。白方击杀比冻结更快，所以黑船基本来不及突防。
5. **USV 可全速 20 m/s**（黑方固定 10）→ 想什么时候打就什么时候打。
6. **40km 锁距 > 35km 探测**：白方"看得见就锁得上"，无锁隙（黑方 30km 雷达 + 40km 锁距其实有 10km 锁隙，但它靠 UAV 补上）。
7. 隐藏漏洞：最后一艘黑船突防仍算 Victory（§1）；超时正分算 Victory（理论，实战不可达）。不依赖它们，但知道它们存在。

---

## 15. Deterministic Winning Strategy v1（确定性最小策略）

**输入**：`white_usv_states`（位置/速度/航向/locking/冻结）+ `white_observation`（黑船探测位）。
**动作原语**（POMDP API `/legal_actions`，`pomdp_api/main.py` L864–945）：`move(speed,course)`、`lock(target)`、`launch_uav`、`fly(speed,course)`、`land_uav`、`noop`。

```
初始化(t=0):
  对每艘白USV: move(target_speed=20, target_course=90)     # 全速东进
  对每架白UAV:  launch_uav(home, 150, 90)                  # 起飞朝东侦察(可选)

控制循环(每 ~5s sim):
  T = 所有已探测黑船(white_observation 中 is_active 的黑USV)
  对每艘白USV u:
      if u 正在锁定: continue                                # 锁定中勿动(idempotent)
      if T 为空:     continue                                # 继续东进
      tgt = argmin_{b∈T} (b 当前被白方锁定数) 按距离取近者     # 优先补位成2v1/3v1
      lock(u, tgt)
  紧急: 若任一黑船 x < 80000 → 所有空闲USV lock 它

终止: get_state().ended_reason == "敌方所有单位被击毁" → Victory
```

**为何确定性获胜**：
- 全速东进使白方在 t≈11000s 进入锁距；2v1/3v1 补位把每次互锁竞速都变成"白必胜"（P11）；
- 15 艘黑船分 2–3 波在 t≈12500–14000s 全灭，早于 21000s 突防期限 8000s；
- 白方每艘至多吃 1 发冻结（可恢复），即使损失几艘仍有 2:1 以上对黑剩余船的兵力优势。
- 紧急规则兜底（实战中几乎不触发）。

---

## 16. 简单高胜率策略（再减一刀）

如果连上面的补位逻辑都觉得多，**最简策略也能赢**：

```
每 ~5s:
  对每艘空闲白USV: 锁定"当前最近的黑船"
  全部USV: move(20, 90)（一次性）
  UAV: 起飞朝东(一次性)
```

原理：15v15 的时间裕度极大（击杀需 ~1500s，期限 21000s），即便偶尔 1v1、偶尔损失白船，只要白方持续把每艘空闲船压到最近黑船上，黑船数量单调下降、白方数量优势增长，最终全灭。**它的风险是白船损失导致漏船，但即使如此，21000s 期限+突防漏洞（§1）双重兜底，实际胜率仍接近必然。** 首推 §15 的 2v1 补位版（额外逻辑 <5 行，把风险压到 0）。

---

## 17. 针对性实验（Hypothesis / Source expectation / Experiment / Observed / Conclusion）

所有实验：短时单进程探针，只读引擎不修改规则，未跑长仿真。脚本：`probe_rules/probe_combat.py`、`probe_v4.py`、`probe_v5.py`、`probe_v6.py`。

**实验1 — 2v1 同锁 300s 窗口击杀分布**
- 假设：两攻击者独立 80% 窗口，+300s P(击杀)=0.64、P(冻结)=0.32、P(无)=0.04。
- 源码预期：locker.py `_locked_work` 独立链 + 300s 严格阈值 + 80% 骰子。
- 实验：w1+w2 同锁 b1，+310s 观测。
- 实测：12 次 → 击杀 8、冻结 4（0.67/0.33，与 0.64/0.32 吻合）。
- **结论：2v1 在首个 300s 窗口 ~2/3 直接击杀，其余必冻结。**

**实验2 — 1v1 击杀节奏**
- 假设：每窗口 80%，杀需 2 发 → ~600–975s。
- 实测：5 次 → 2 次首窗口命中+冻结并在 ~620s 击杀；3 次 miss→重锁→~900s 命中1发。
- **结论：1v1 太慢，且互相竞速有输面；不要打 1v1。**

**实验3 — 命中后攻击者释放可换目标**
- 源码：`_locked_work` 无条件释放攻击者。实测 5/5：+310s 后 `w1.locking=False`，换锁 b2 成功。
- **结论：每个 300s 窗口后攻击者空闲，可重分配火力（分波策略可行）。**

**实验4 — 冻结目标可追刀**
- 源码：frozen 仍在 target 池。实测 5/5：冻结中再锁，+305s 击杀。
- **结论：冻结≠安全，冻结中的目标可被快速补刀。防御上要保护冻结白船。**

**实验5 — UAV 目指 → USV 锁自身雷达外目标**
- 假设：UAV 探测进白方目标池，USV 40km 内可锁。
- 实测 3/3：白 USV 在 38.6km（>35km 雷达）锁黑船，+310s 命中冻结。
- **结论：UAV 把白方锁距从"自身雷达 35km"扩展到"锁距 40km"，确认可行。**

**实验6 — 被锁后拉距离能否脱锁**
- 源码：`dist>=40000` 断锁；但 300s 窗口先到。
- 实测：20 m/s 逃 310s 仅到 20.7km（距 40km 差一半）；+310s 即被命中冻结（t0/t2）。瞬移 >40km（P6b）2/2 秒断。
- **结论：实战中脱锁必先撞上 300s 命中窗口，逃跑无效；正确反应是集中反杀。**

**实验7 — UAV ±30° 扇区与 30s 黏性**
- 实测：朝东 90° 扇区[60,120] 只收 78° 目标；转 NE45° 扇区[15,75] 只收 51° 目标；转后 15s 旧目标仍在 intel（黏性），+31s 过期。
- **结论：扇区锚定航向；探测有 ≤30s 黏性；UAV 必须飞行转向。**

**实验8 — 黑 UAV 延长黑 USV 锁距（对称验证）**
- 实测 2/2：黑 UAV 朝西飞探测白船（方位243°∈[240,300]），黑 USV 在 35.2km（>30km 黑雷达）锁定白船。
- **结论：黑方同样享有 UAV 延长锁距，白方不能指望"黑船看不见"；但黑方火力分散（1v1）仍必败。**

**实验9 — 互锁竞速 2v1 vs 1v1**
- 假设：白 2 发先到（+300–600s），黑 1v1 第 2 发要 +600s，且黑先被冻结失去反击。
- 实测 3/3：黑船 +350s 死亡，白方 1 艘只吃 1 发冻结（存活），黑方无击杀。
- **结论：集中火力在互锁竞速中必胜——这就是本报告的胜负核心。**

**实验10 — 突防判定与结束顺序**
- 实测：x=49000 黑船被记突防并击杀；单黑船场景下 `ended_reason=敌方所有单位被击毁`（因 `black_ship_alive==0` 先于 `breakthrough`）。
- **结论：白方唯一需要的就是"在首个突防前杀光黑船"；最后一艘突防仍是 Victory（保底）。**

**实验11 — 运动学**
- 实测：USV 加速 2 m/s²（~10s 到 20）、转向恰好 5°/s（180° 用 36s）；黑船 `cmd_sail_area` 精确 10 m/s 西进（1600s→16000m）；黑 UAV 30 m/s 西进；白 UAV 起飞后 20s 达 150 m/s。
- **结论：运动学确定；白方可用 20 m/s 全速东进控制交战时机。**

---

## === WINNING STRATEGY SUMMARY ===

**这个游戏怎么赢（一句话）：白方把 15 艘 USV 全部全速东进（20 m/s），以 2v1（首选 3v1）集中锁定黑船，每约 300–600 秒歼灭一艘，在首个黑船突防（t≈21000s）之前（t≈13000s 完成）杀光全部 15 艘黑船 → `敌方所有单位被击毁` → Victory。**

**胜负机制（已用源码+实验双重验证）：**
- 击杀需 **2 发命中**；每攻击者 **300s 独立窗口**、**80% 命中**、**第 1 发即冻结 300s**（不能动/不能开火/不能反击）；2 发攒满即杀。→ 2v1 在 300s 有 64% 直接击杀、600s 累计 ~93%；1v1 需 ~600–975s；3v1 在 300s 达 ~90%。
- 白方探测 35km 全向 / 黑方 30km 全向 / 双方 UAV 60km±30°；UAV 探测进入**全队目标池**，使任意 USV 可在 **40km 锁距** 内锁定任何被看见的黑船（P5 实测 38.6km 成功；黑方对称，P10 实测）。
- 黑方 **完全确定性**：15 艘 USV 直线西进 10 m/s、15 架 UAV 西进 30 m/s、`black_strategy` 每艘 1v1 分散锁定、无规避无集中无 UAV 指挥。→ 白方集中火力在**每次互锁竞速中必胜**（P11 实测 3/3），每艘白船至多吃 1 发冻结（不致死）。

**最低成本可行策略（确定性，无需扇区/阵型/规避/复杂逻辑）：**
1. 一次性：全部白 USV `move(20, 90)` 全速东进；全部白 UAV 起飞 `fly(150, 90)` 朝东（可选）。
2. 每 ~5s：对每艘**空闲**白 USV，锁定**当前被白方锁定数最少的最近黑船**（自动形成 2v1/3v1 补位）；锁定中的不打扰（指令幂等）。
3. 紧急兜底：任一黑船 x<80000 → 空闲 USV 全部压上。
4. 直到 `get_state` 返回 `敌方所有单位被击毁` → Victory。

**时间/数量账（为何必赢）：** 首次锁定 t≈11000–11250s → 分 2–3 波歼灭 t≈12500–14000s → 比首个突防期限 21000s 提前 ~8000s；白方即使损失数艘仍保持对剩余黑船的 2:1 以上兵力优势。**失败唯一途径是白方在 21000s 前既没杀光黑船又出现漏船突防——集中火力策略把这个概率压到接近零。**

**不推荐做的复杂化（回应"不要过度设计"）：** 不需要 UAV 战术扇区规划（UAV 只是可选早期预警）、不需要拦截线/阵型/驻点、不需要规避机动（锁不住逃不了，冻结可存活）、不需要概率对冲——**唯一的战术要点就是"集中火力、赶时间杀光"。**
